# This rule is not used by the project growi-manager itself.
# It is provided to allow the cluster admin to help manage permissions for users.
#
# Grants read-only access to app.maeshinshin.github.io resources.
# This role is intended for users who need visibility into these resources
# without permissions to modify them. It is ideal for monitoring purposes and limited-access viewing.

apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  labels:
    app.kubernetes.io/name: growi-manager
    app.kubernetes.io/managed-by: kustomize
  name: growi-viewer-role
rules:
- apiGroups:
  - app.maeshinshin.github.io
  resources:
  - growis
  verbs:
  - get
  - list
  - watch
- apiGroups:
  - app.maeshinshin.github.io
  resources:
  - growis/status
  verbs:
  - get
