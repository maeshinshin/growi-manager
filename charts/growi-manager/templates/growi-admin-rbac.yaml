apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: {{ include "growi-manager.fullname" . }}-growi-admin-role
  labels:
  {{- include "growi-manager.labels" . | nindent 4 }}
rules:
- apiGroups:
  - apps.maesh.dev
  resources:
  - growis
  verbs:
  - '*'
- apiGroups:
  - apps.maesh.dev
  resources:
  - growis/status
  verbs:
  - get
