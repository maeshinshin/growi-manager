apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: {{ include "growi-manager.fullname" . }}-serving-cert
  labels:
  {{- include "growi-manager.labels" . | nindent 4 }}
spec:
  dnsNames:
  - '{{ include "growi-manager.fullname" . }}-webhook-service.{{ .Release.Namespace
    }}.svc'
  - '{{ include "growi-manager.fullname" . }}-webhook-service.{{ .Release.Namespace
    }}.svc.{{ .Values.kubernetesClusterDomain }}'
  issuerRef:
    kind: Issuer
    name: '{{ include "growi-manager.fullname" . }}-selfsigned-issuer'
  secretName: webhook-server-cert
